{
  "name": "soyte",
  "private": true,
  "version": "1.0.0",
  "description": "Sở Y Tế Hà Nội — Healthcare Portal Monorepo",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "clean": "turbo run clean && rimraf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.4",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3",
    "@types/node": "^22.15.29"
  }
}
